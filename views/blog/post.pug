.container
    .row
        .col-lg-8.col-lg-offset-2.col-md-10.col-md-offset-1
            h2 #{post.title}
            h5 #{post.subtitle}
script.
    updateBrowserData('#{post.title}', '#{url}', #{ajax}, #{back}, #{noUpdate});